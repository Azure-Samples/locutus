$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
name: base-hf-transformer-train
description: Environment for huggingface transformer training based on an azureml base image.
version: 5
os_type: linux
build:
  path: docker-context
  dockerfile_path: base_env.dockerfile
